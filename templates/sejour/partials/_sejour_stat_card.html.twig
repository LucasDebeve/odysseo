<div class="bg-white p-5 rounded-3xl border border-gray-100 shadow-sm flex items-center justify-between">
    <div>
        <p class="text-[10px] font-black uppercase text-gray-400 tracking-widest">{{ label }}</p>
        <p class="text-2xl font-black">{{ val }}</p>
    </div>
    <div class="p-3 {{ color }} rounded-2xl">
        {{ ux_icon(icon, {class: 'w-6 h-6 text-gray-700'}) }}
    </div>
</div>